export enum CellType {
  Empty,
  Block,
  Food,
  SpecialFood,
  SnakeBody,
  SnakeHead,
  SnakeMouthOpen,
  SnakeTail
}
export enum GameStates {
  None,
  Running,
  Paused,
  GameOver,
  Win
}

export enum Direction {
Up=1,
  Down,
  Left,
  Right
}

